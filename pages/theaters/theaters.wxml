<import src="../template/list"/>
<view class="container">
  <view class="list-title">正在上映</view>
  <template is="theaters" data="{{ movies }}"/>
    <view wx:if="{{showNoResult}}" class='no-result'>没有更多内容了</view>
</view>
